<!--carregando partials-->
<%- include('partials/header.ejs') %>
<%- include('partials/homenavbar.ejs') %>
<body>
    <div class="container">
        <div class="jumbotron">
            <h1 class="display-4">Seja bem-vindo!</h1>
            <p class="lead">encontre aqui respostas para sua jornada em programação!</p>
            <hr class="my-4">
            <p>temos centenas de artigos que podem te auxiliar nessa caminhada!</p>
            <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
          </div>

        <% articles.forEach(article => { %> 
           <div class="card">
               <div class="card-header">
                    <h2><%=article.title%></h2>
               </div>
               <div class="card-body">
                    <a href="/<%=article.slug%>" class="btn btn-primary">ler artigo</a>
               </div>
           </div>
           <br>
        <%})%>

    </div>
</body>
<%- include('partials/footer.ejs') %>